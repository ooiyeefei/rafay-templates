# Generated by create-runai-cluster.sh
cluster_uuid.txt
client_secret.txt
control_plane_url.txt

# Temporary kubeconfig
/tmp/kubeconfig-*
kubeconfig.yaml

# NOTE: cluster-issuer.yaml and runai-ingress.yaml are committed as placeholders
# They are overwritten by local_file resources during apply
# This prevents "file not found" errors during Rafay provider refresh

# Downloaded binaries (from setup.sh)
curl
jq
kubectl

# Terraform files
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
.terraform/
.terraform.lock.hcl
