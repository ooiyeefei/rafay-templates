# Generated by create-runai-cluster.sh
cluster_uuid.txt
client_secret.txt
control_plane_url.txt

# These files are no longer generated (simplified approach)
# project_id.txt - removed (no project creation)
# user_email.txt - removed (use var.user_email)
# user_password.txt - removed (shown in script output)

# Temporary kubeconfig
/tmp/kubeconfig-*
kubeconfig.yaml

# NOTE: cluster-issuer.yaml and runai-ingress.yaml are committed as placeholders
# They are overwritten by local_file resources during apply
# This prevents "file not found" errors during Rafay provider refresh

# Downloaded binaries (from setup.sh)
jq
kubectl
# Note: BusyBox wget is available in Rafay container, no need to download

# Terraform files
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example
.terraform/
.terraform.lock.hcl
